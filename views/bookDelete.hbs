<h1>{{title}}</h1>
{{#if bookInstances}}
<p><strong>This book cannot be deleted as there are copies of it.</strong></p>
<ul>
  {{#each bookInstances}}
  <li><a href={{this.url}}>{{this._id}}</a></li>
  {{/each}}
</ul>
{{else}}
<p>Are you sure you want to delete this book?</p>
<form action="" method="POST">
  <button type="submit" class="btn btn-primary">Delete</button>
</form>
{{/if}}